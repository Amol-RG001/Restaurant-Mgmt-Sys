@model IEnumerable<FoodRecipe.Models.FoodCategory>

@{
    ViewData["Title"] = "IndexCustomized";
}

<h1>Food Category Menu</h1>


<div class="card-columns">
    @foreach (var fooditem in Model)
    {
        <div class="card border border-danger">
            <div class="card-header font-weight-bold">
                @fooditem.FoodCategoryName
            </div>
            <div class="card-body ">
                @Html.DisplayNameFor(modelItem => fooditem.FoodCategoryName) : @fooditem.FoodCategoryName
                @*@Html.DisplayNameFor(modelItem => fooditem.FoodCategory) : @fooditem.FoodCategoryName*@
                <br />
             
            </div>
            @*<div class="card-footer">
        <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@fooditem.FoodItemId">Edit</a>
        <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@fooditem.FoodItemId">Details</a>
        <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@fooditem.FoodItemId">Delete</a>
        </div>*@
        </div>
    }
</div>
<a class="btn btn-sm btn-secondary" asp-area="Recipe" asp-controller="ShowFoodCategories" asp-action="Index">Back to Menu</a>